#ifndef _HANDLEINTERRUPT_H
#define _HANDLEINTERRUPT_H

/* The interrupt handler which is called by the interrupt function, which is
   the actual entry point of an interrupt. */
void handleinterrupt(void);

#endif
